[package]
name = "trackpad-guard"
version = "0.1.0"
edition = "2021"
description = "Disable sway touchpad while typing on USB combo keyboard/touchpad devices (e.g. AMIRA in Argon ONE UP)"
license = "Apache-2.0"

[dependencies]
evdev = "0.13"
ctrlc = "3"
