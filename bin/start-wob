#!/bin/bash
pkill -x wob 2>/dev/null
pkill -f "tail -f.*wob.sock" 2>/dev/null
sleep 0.3
WOBSOCK="${XDG_RUNTIME_DIR:-/run/user/$(id -u)}/wob.sock"
rm -f "$WOBSOCK"
mkfifo "$WOBSOCK"
tail -f "$WOBSOCK" | wob
