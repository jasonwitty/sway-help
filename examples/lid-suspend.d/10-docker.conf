# Stop Docker daemon on lid close — zero containers running is pure overhead
[close]
sudo systemctl stop docker.service containerd.service

[open]
sudo systemctl start containerd.service docker.service
